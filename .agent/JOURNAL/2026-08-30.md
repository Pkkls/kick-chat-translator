# 2026-08-30

## What this pass built

The working system itself: `.agent/`, a standing prompt with no dated fact in
it, a state file nobody writes by hand, a queue, and this journal. No file under
`src/` changed.

## Why

A handoff written on 2026-08-27 opened a session on 2026-08-29 with four claims
about the repository. All four were wrong: it said the tree was clean (thirteen
modified files), that HEAD was on `196b441` (five commits further on), that the
version was 2.8.0 (2.8.1), and that the offline gates ran in series and
parallelising them was the first untouched opportunity (`run-gates.mjs` had been
pooling one worker per core, with a `--jobs` flag, all along).

The lesson is not that the author was careless. It is that prose about a
repository decays at the speed the repository moves, and that a frame and a
state cannot live in the same file. So `PROMPT.md` is forbidden from containing
a version, a commit, a count or a current open point, and `ETAT.json` is
forbidden from being edited.

## What the generator found on its first run

Three things nobody had asked it for, which is the argument for having it.

**A third remote.** `public` and `origin` point at the same URL. `public`'s
tracking ref is stale, so it reported 16 commits ahead for a repository `origin`
reported up to date. Every look at the remotes this week had used
`git remote -v | head -2` or `head -4`, which cut before the third line: the
"there is no origin" claim made earlier that day came from the truncation, not
from the repository. `state.mjs` now names remotes that share a URL.

**Nineteen orphan harnesses of thirty-three.** The runner enumerates eighteen
gates. Everything written since is on disk and in no runner, including the three
live harnesses that are the only coverage of the language UI reworked this week.

**Two bugs in the generator, caught by reading its own output.** It reported
thirty-six gates because the regex matched every quoted string in the block,
counting each gate's script path as a gate. And it printed `undefined ouvert(s)`
because `PLAN.md` did not exist yet and the absent case returned no counters.
Both fixed before the file was used for anything.

## Probes that were wrong before the product was

Kept because this is the list that saves the next pass.

- A panel reported off-screen. It overshot by 0.2px, subpixel rounding, against
  a chat column that itself ends at the window edge. The tolerance was the bug.
- A keyboard probe reported the panel's arrow navigation dead. It demanded focus
  land on a list row; Down from the filter field lands on the favourites strip
  above the list, which is correct. Dispatching the event inside the page
  instead of through the driver is what separated "the key never arrived" from
  "the listener is not there".
- A search for three markers inside a zip, run over the compressed bytes. It
  found nothing, in every archive, including ones that certainly contained them.
  Inflating the entries first turned it into a real control.
- A transition duration of `0s` filed as a defect against the art direction. The
  element is shown and hidden with `hidden`; a transition there never plays. The
  finding was mine, not the code's.

## The gate suite had been red for a day, and nobody had looked

Running `run-gates.mjs` for the first time this week returned exit 1 on two of
its eighteen gates. Both had been failing since `62b669e`, committed the day
before.

**`audit-rtl`** flagged `right: auto` in `.kt-lang-panel-fixed`. The audit
already carried that exact exception for `.kt-chip-menu-fixed`, with the reason
written next to it: the placement function writes viewport coordinates, which
are physical by definition, and the logical property above must not fight them.
The panel arrived later with the identical construct and was never added. Stale
allowlist, not a defect. Fixed, and checked both ways: with the exception the
audit exits 0, without it exits 1.

**`chip-live`** failed six assertions across three renders, all of the form "a
click on the code opens the list instead of toggling". That is the change made
on request the same day. The gate encoded the old contract, exactly as three
unit tests did, and those were retargeted while this one was not, because the
unit tests run in `npm test` and this does not. Retargeted the same way: a click
opens wherever it lands, the next click closes. Both assertions kept and turned
around, since keeping only one would pass a chip that opens and never closes.

**`audit-da`** then surfaced three findings, all on the drawn flags: an inset
hairline in each theme and a 2px radius. The direction bans the drop shadow;
`inset` is a line painted inside the box, and without it Japan's flag is an
invisible white rectangle on `#171A1C`. A 4px radius on a box 12px tall eats a
third of its height. The audit had no way to express an exception, so one was
added, narrow: that selector, those two properties. Checked against six cases,
six correct, including that a shadow on `.kt-compose` and a radius on
`.kt-chip-menu` are still caught, and that duration on `.kt-flag` still is.

Result: 18/18 green, 19.2s of wall clock against 106s of cumulative work, six
workers, a factor of 5.52.

**What this says about today.** "Gates green" was said several times over this
session meaning typecheck, lint, test and build. The eighteen-gate suite was
never run once, and two of its gates were red the whole time, one of them made
red by a change made that day. Four commands are not the gates; they are four of
them.

## The apparatus does not survive a machine

`git ls-files scratchpad/` returns 0. Every gate, every harness and every audit
is under a directory `.gitignore` excludes at line 18. They exist here and
nowhere else, and a fresh clone of the public repository gets none of them,
including the runner `PROMPT.md` instructs the next session to run.

State that survives a session limit is worth little if the means of checking it
does not survive a machine. It is the first open item in `PLAN.md`, priced
rather than fixed: moving it in publishes every measurement script and the
screenshots they write, which is a decision about what the public repository is
for, and that decision is kil's.

## Verification run on the system itself

1. Cold start: `node .agent/state.mjs --texte` answers branch, HEAD, tree, per
   remote divergence, both versions and their agreement, every archive's
   manifest version read by inflating it, the gate count and the orphans, the
   release as an unauthenticated client sees it, and the queue counters.
2. The counters read 0 open and 0 blocked before `PLAN.md` existed, and the real
   numbers after. The absent case returns numbers rather than holes.
3. The gate parser reports 18 after the fix, matching the `GATES` array by hand.

## Second pass: the apparatus now survives a machine

Taking the first open item, which was the one that undermined the rest.

**What made it hard, measured before deciding.** Playwright is not a dependency
of this project, and 33 harnesses each carried the same absolute path into one
home directory as its fallback location. There is CI, two jobs on node 20, and
it never runs these gates: adding Playwright as a devDependency would pull
browser binaries into both installs for nothing. And the split that settled what
to track: 422 KB of scripts against 272 MB of screenshots, HTML dumps, bundles
and browser profiles.

**What was done.** A single `scratchpad/harness/playwright.mjs` resolves the
browser from `$UX_KIT`, then from a gitignored `scratchpad/uxkit.path`, then
from the repository's own `node_modules`, and exits 2 when none of the three is
there, printing all three ways to fix it. Exit 2 and not 1, because a machine
without Playwright is a missing prerequisite and not a failed gate, and a runner
that cannot tell those apart reports the first as a product defect. The 33
harnesses import it. `.gitignore` re-includes `*.mjs` and `audit_*.py` under
`scratchpad/` and nothing else, so 40 files and 291 KB enter and every output
stays out, along with the French working prompts.

**Three things went wrong doing it, all mine.** The bulk rewrite missed
`snapshot.mjs`, whose two lines were written on one line. It missed
`measure-popup.mjs`, which had its own home-made resolver, and then turned
`const chromium = await loadChromium();` into `const chromium = chromium;`,
which the gate caught as "Identifier 'chromium' has already been declared". And
the first gate run after the rewrite failed with exit 2 everywhere, correctly:
the private path was gone and nothing had replaced it yet, which is what the
`uxkit.path` file is for.

**Verification.** `node --check` on all 35 harnesses, then 18/18 gates green,
19.5s of wall clock against 114.2s cumulative, factor 5.86. Then typecheck,
lint, 620/620 tests and build. The gates are the verification of a mechanical
rewrite across 33 files; reading the diff would not have caught
`const chromium = chromium;` as fast as running it did.

**Left open on purpose.** 19 harnesses of 35 are still in no runner, and the
live ones still have no entry point. Tracking them was the prerequisite for
fixing that, not the fix.

## Third pass: a harness nobody ran was reporting a real bug

Taking the runner-coverage item. Classifying the orphans first, by measurement
rather than by name: 6 offline and 13 live, and of the 6 offline, 3 assert
nothing at all. Those three draw images and print numbers for a human;
wiring them would buy runtime and no verdict, so they stay out and the runner
now says so in place.

Of the three offline orphans that do assert, two passed and were wired.
`bar-panel-live` failed on three counts. Two were the old contract, the same
retargeting as the unit tests: it read the favourite tiles' visible text, which
was removed the day before, and it capped the panel at 45% of the chat when the
panel had deliberately grown to show 25 rows instead of 6. The third was
`the panel leaves the window`, and it was real.

**The bug it was pointing at.** Replacing that boolean with the four edges, as
this journal already argued for once today, gave `gauche: -4`. Then the live
harness at three window sizes: clean at 1500x950 and 1280x900, and at 1000x750
the panel sat at y=711 with `bas: -362.2`, a third of the list below the bottom
of the screen. Instrumenting the placement showed the inline style saying
`top: 222px` while the element rendered at 711, which is not a placement error
at all: a `position: fixed` element is only viewport-relative while no ancestor
establishes a containing block, and walking the ancestors named the culprit as
`div.kt-float`, carrying a `backdrop-filter`. The bar's own top is 489, and
489 + 222 = 711 to the pixel. Our CSS, not Kick's.

The fix is the one already used twice this week for the same family of problem:
hang the panel off the body. It also survives Kick adding a transform above the
bar, which no amount of care inside the bar would.

I speculated twice about why before instrumenting, and stopped on the second,
which is the rule. Reasoning about `left: 206px` rendering at -4 was never going
to converge; asking the DOM which ancestor stole the containing block took one
probe.

## Four ways I broke my own probes this pass

Kept because the pattern is the point: most red is mine before it is the
product's.

- The bulk rewrite of 33 harnesses produced `const chromium = chromium;` in the
  one file that had its own resolver.
- The keyboard probe selected `.kt-lang-panel` with `querySelector` after the
  panel moved to the body, where a hidden leftover can come first. It crashed on
  a null instead of measuring, and a crashed probe reads as a red gate.
- A python heredoc ate a `\n` and split a JS string across two lines, in the
  runner, an hour after writing down that heredocs do this.
- The live runner labelled every exit 2 as "Playwright missing". `latency` uses
  the same code for "dist was not built with metrics". Two prerequisites, one
  number, and a label that named only the first. It prints the reason now
  instead of guessing it.

## The live suite, run whole for the first time

6 of 7 green, 818s of wall clock against 1077s cumulative on two workers, plus
one prerequisite: `latency` needs `npm run build:metrics` while every other gate
needs the ordinary build, so a normal pass can never run it. It exits 2 and says
so, which is the honest outcome and also means nothing ever exercises it.

The one failure is `live-kick`, reporting zero translations and zero errors with
a French target on a channel carrying 27 lines. That is the shape the frame
warns about, a pass that measured nothing looking like a pass, except this one
fails on it rather than reporting green: the assertion is doing its job. It
passed in the run before this one, which makes it evidence for the
non-determinism item rather than a regression to chase now.

## What the live suite says about itself

It is not deterministic. Two runs minutes apart disagreed on `kick-dom-recon`
and `bar-panel-mesure`, which follow whichever channel `/browse` serves. Three
consecutive runs of `bar-panel-mesure` after the null fix were green, so that
one was a crash and not noise, but the general point stands and is now an open
item: a suite that goes red on a quiet channel is a suite people learn to skip.

## Fourth pass: two gates that could not fire, and one that now can

**`latency` had never run.** It reads counters only a metrics build emits, and
`check-strip.ts` fails a release build that keeps them, so an ordinary dist
carries none. It detected that and exited 2 with the reason, which is the right
refusal and also meant every pass skipped it forever. It has its own phase in
`run-live.mjs` now: last, alone because it rewrites dist underneath everything
else, building metrics, measuring, and restoring the ordinary build in a
`finally` so a failed measurement cannot leave counters in a package. First run
in its life: green, 108.3s.

**The node version is no longer something to remember.** The 2.9.x packages went
out built on 22 while `.nvmrc` pins 20 and CI builds on 20; `engines: >=20`
allows both, so nothing anywhere objected and it surfaced only while writing
reviewer notes that had to state a version. `pack.ts` prints the running version
and the full sha256 with every archive and says when it differs from the pin. A
warning rather than a failure, because refusing would have the packer contradict
`engines`.

**And that printing immediately caught something worse.** The sha of the freshly
packed 2.9.2 was not the sha of the published 2.9.2: rebuilding after the
placement fix had overwritten it. Same version number, a different build, which
is precisely how a stale package reaches a store. `state.mjs` compares every
local archive's size against the published release now and says so at every
start; local chromium is 294379 bytes against 294586, a delta of 207. The
condition itself stays open until this branch merges and a 2.9.3 ships, and
nothing in `release/` should be submitted while the warning is up.

## Fifth pass: the flapping was a mute probe, and I had filed it as weather

The queue said "the live gates are not deterministic", with the channel, the
chat volume and the network as the suspects. That framing was mine and it was
wrong in the most useful way: it turned three separate defects into one property
nobody can fix.

`live-kick` was setting `.value = 'fr'` on the bar's language control and
dispatching a `change` event. That control stopped being a `<select>` when the
shared panel replaced it, and `langMenu.ts` documents the replacement in its own
header. On a `<button>`, both lines do nothing. So the target was never changed,
and the translation count that followed was counting whatever the default target
had produced: it came out above zero when `/browse` served a channel in a
language the browser default differed from, and zero when it did not. Weather,
apparently. A probe that cannot perform the action it is testing.

It drives the real control now: click the button, take French from the panel,
and read the control's own label back before counting anything. Zero after a
target that never moved and zero after a broken pipeline are the same number,
and only the label separates them. Three runs: 6, 5 and 2 translations, zero
errors, target FR each time. The verdict is steady and the count follows the
traffic, which is the right way round.

Of the other two flaps, one was a null crash in my own keyboard probe, fixed
earlier today, and one was a single unexplained `kick-dom-recon`. So the
systemic claim rested on one unknown and two of my own bugs. Filing weather is
easier than reading three stack traces, and it costs the next reader the chance
to fix anything.

## Sixth pass: three more mute probes, and a question the data cannot answer

**The same defect, in two more harnesses.** After `live-kick` was found setting
`.value` on a button, a static sweep for the pattern found `live-fallback` and
`live-recycle` doing exactly the same thing, both reporting green. Both carried
a comment saying why the target had to change, one of them word for word: "or
nothing is ever translated and every check below passes on an empty set". The
author knew the trap and the code walked into it three times, because the
gesture was copied instead of shared. It lives in one module now, so the next
time that control changes shape one file breaks loudly.

**A second sweep, for a second signature.** Selectors the harnesses query that
the product can no longer emit. Two real ones: `live-kick` counted a class
deleted in 2.8.0, so its assertion could not fail and had not been able to for
two versions; `chip-live` looked for a class that never existed, behind a `??`
fallback that quietly did the work, so nothing ever noticed the selector die.
The sweep is a gate now, and it went from two findings to zero on the same code,
which is its witness. It then found two more dead reads in a harness edited
earlier today, with the contrast assertions that depended on them: assertions
gated on a value that is always null are assertions that cannot fire.

**And a direction that had to be declined.** Store analytics were offered as a
basis for optimisation. They measure the listing page: a sixth of that traffic
is on phones and tablets, another eighth on browsers that cannot install a
Chrome extension, and there is no install or usage figure anywhere in them.
They can inform the listing and they cannot rank a code change. The extension
ships no telemetry on purpose, which is stated in its own store text, so the
only runtime numbers that exist here are the ones a harness takes. Saying that
plainly is worth more than producing a plausible optimisation from data that
does not contain the answer.

## Seventh pass: measuring the only thing that could be measured

The analytics could not rank a code change, so the question became what in this
repository can be. The answer is the injected script: `assets/content.js` is
downloaded, parsed and run on every kick.com page whether anyone opens anything
or not, and `inject.css` is inlined into it. It is the only cost the extension
imposes on everyone all the time.

Read out of the published archives rather than out of the handoff, which had a
figure for it that was already stale: 199.5, 199.4, 199.4, then 224.3 at 2.9.0.
Twelve and a half percent in one version, for the drawn flags and the shared
panel, sitting there for three days because nothing looked. That growth is mine.

`audit_poids.py` reports the delta every run and fails past a two percent
margin. Its baseline is a constant in the file rather than a JSON beside it,
because `scratchpad/` only tracks `*.mjs` and `audit_*.py` and a data file there
would be gitignored: the baseline would not survive a machine, which is the
mistake this directory exists to stop repeating. Green now, red against a
baseline twelve percent lower, which is the jump it would have caught.

What was measured and then not acted on: the flag rules are 10.9 KB of the
bundle, 45 rules carrying 7.5 KB of SVG data URIs, 18.7 percent of the CSS.
Injecting them lazily on first list open would save 4.9 percent of the bundle
and a parse cost under a millisecond, at the price of a second stylesheet and a
flash risk. Measuring it was worth it; doing it would have been optimising a
number.

## Eighth pass: a fifth analytics file, and two things I had asserted wrongly

A further export arrived and corrected me twice.

**There is an install figure**, in the event block: eighty-five installs against
two hundred and ten first visits, 40.5 percent, over eight months. The previous
pass stated flatly that no install or usage figure existed anywhere in the data.
It existed, in a file that had not been handed over yet, and the honest thing is
that "the data does not contain it" was a claim about four files stated as a
claim about the subject.

**And the language gap was measured, not inferred.** The earlier pass mapped
country to language and named Polish as the largest uncovered group. This export
carries the page titles: which localised store page each view landed on. English
227, Turkish 30, Arabic 18, Czech 14, Russian 6, Japanese 5, Chinese 3, Korean
2. Polish appears on none of them. Czech is fourth, ahead of four languages that
already had listing text, and it had none. Inference produced a plausible answer
and direct measurement produced a different one; the direct measurement wins,
and the inference should not have been filed as a finding in the first place.

Czech was written for both stores. Its derived AMO description came out at
fifteen paragraphs with two GitHub mentions, exactly the duplication the other
eight had, and the shape check caught it on the first run. Eleven languages each
side, 34 fields inside their limits.

The claim that held: this is listing traffic, not runtime. A sixth on phones, an
eighth on browsers that cannot install a Chrome extension, and new users equal
active users every single week, which is the signature of visitors rather than
an installed base.

## Ninth pass: the kit's own gate accused working code

Two menus were redesigned today and no accessibility audit had been run on
either. The kit has three: axe, target size, keyboard.

**axe: 0 violations** on the chip menu in both themes. Checked that it can fail
here rather than assumed: a button with no accessible name inserted into a copy
of the same page gets reported CRITICAL and exits 1, so the audit does look at
this page.

**Target size: 96 targets, all at or above 24x24 or covered by an exception.**

**Keyboard: FAIL**, reporting that `div#kt-lang-list` uses a roving tabindex and
that the arrows never move focus. That is true of the file and false of the
product. The gate reads this harness's saved HTML, which carries the rendered
DOM and none of its listeners, because the component is mounted through
`page.evaluate` and that call is not in the saved page.

So the measurement was taken where the listeners exist. In all three renders,
dark, light and RTL: Down +3, Right +1, Up -3, and the grid reporting 3 columns.
The chip's grid keyboard does exactly what it was built to do, and it had only
ever been checked by unit tests until now.

Those three numbers are asserted now rather than printed. Witness: forcing
`MENU_COLS` to 1 fires the assertion on all three renders, on the column count
and on both vertical steps, exit 1; restoring gives exit 0.

That is the fifth probe today to accuse working code, and the first that was not
mine. A static dump is a fair thing to audit for structure, roles and contrast,
and it cannot answer a question about behaviour.


## Pass ten: the bar panel gets the same treatment, and the audit stops being a one-off

The chip menu was audited by hand. Run once, green once, and nothing would have
run it again. This pass gives the bar panel the same three gates and then
attaches all of them to the harnesses so they run on every sweep.

**The bar panel had no auditable artefact.** `lang-panel.html` was a
hand-written two-row mock of a forty-three-row panel, stale enough to be
misleading. The harness writes a real dump now, taken from the rendered page.

First audit: two axe violations, `document-title` and `html-has-lang`, both
against my own test stage rather than the product, and 14 targets. Fourteen for
a panel with thirty-nine rows.

I guessed `tabindex` had not survived serialisation. Measuring refuted it: 48
`tabindex` attributes sit in the file. The real cause was that the dump was
taken straight after the filter test, with "por" still in the box, leaving 2 of
39 rows visible. The gate was auditing a typing state.

Clearing the filter gives 96 targets. Adding a `title` and `lang` to the stage
clears both violations. **axe: 0 violations. Target size: 96 targets, all at or
above 24x24.**

**The trap that finding leaves behind.** The target-size gate passes just as
happily on 2 targets as on 96: it measures the targets it finds, it does not
count them. A filtered dump is green. So the harness measures visible rows in
the page at the moment it writes the file and fails if that is not every row.
Witness: moving the dump back before the filter is cleared gives `le dump audite
montre 2 rangees sur 39`, exit 1, while axe stays green on the same run. The
accessibility gates could not have caught this on their own.

A first attempt at that assertion counted rows in the HTML file and passed in
both states, because the stylesheet is inlined in the dump and its selectors
count as matches. A first attempt at the witness exited 1 for the wrong reason,
a Playwright timeout, since removing the filter clear also hides the tile the
harness clicks next. Neither was a witness until it failed for the stated
reason.

**The audits are attached to the harnesses, not added as a gate.** They need a
rendered HTML file, and the files are written by the harnesses. `run-gates.mjs`
pools its work, so a separate gate could run before its input existed and report
a missing file as a failure. `a11y.mjs` is called by `chip-live` and
`bar-panel-live` on their own dumps, which orders it by construction. Four dumps
audited per sweep. Witness: forcing a dark-theme contrast failure reports
`SERIOUS color-contrast (43 nodes)` on the dark and RTL renders and exits 1,
while the light render stays green, which is correct since the forced colour
passes on a light ground.

Cost: `chip-live` goes to 19.3s and becomes the slowest gate, so it now sets the
suite's wall clock, which was 14.9s of `measure-popup` before. The whole sweep
pays about 4.4s.

**A gate was red and nothing said so.** `npm run lint` fails on
`scripts/kick-chat-collector.js`, added in `e3ea484`. `tsconfig` includes
`scripts` without `allowJs`, so the type-aware parser finds the file in no
project and the whole run dies on it. It is a snippet meant to be pasted into a
browser console, not a module of the package, so it joins `scripts/pack.ts` in
`ignorePatterns`. Witness: an unused variable in `flags.ts` still exits 1.

**Lost work, recovered by hand.** Chasing whether that lint failure predated my
changes, I ran `git checkout HEAD -- .` after applying a stash and then dropped
the stash. Both modified harnesses went back to `HEAD` and the stash reflog was
empty. The edits were small and known, so they were redone rather than hunted
for. `git checkout -- .` is not a way to ask a question about history.

23/23 offline gates, typecheck 0, lint 0, 620/620 tests.


## Pass eleven: the gates stop depending on a browser this machine happens to have

Last pass made the apparatus tracked so a clone could run it, and removed 33
absolute paths pointing into one home directory. It left the larger dependency
in place: 22 of 38 harnesses launched `channel: 'chrome'`, which is Google
Chrome as installed on the machine, and 17 of those are gates. A clone with
Playwright installed and no Chrome runs almost none of the offline suite.

Measured before touching anything. No harness uses a Chrome-only API, so nothing
was relying on the real browser for behaviour. Both browsers exist here,
151.0.7922.34 bundled against 151.0.7922.174 system, and one gate passed under
the bundled one unchanged.

The split was taken from the `GATES` list rather than by eye, and the three
offline shooters added by name. `live-kick` and `compose-kick-live` keep the
system Chrome: they open the real kick.com, where the profile and the site's bot
detection are part of what is being measured.

**The system Chrome is faster and that is not what decided it.** One gate, three
launches each: 914, 1068, 1443 ms system against 1474, 1899, 2256 ms bundled,
roughly half a second more per gate. The suite's own wall clock could not settle
the question, since two runs of the same command gave 44.0s and 23.7s. What
decides it is that an apparatus which may run on either browser produces two
sets of numbers, and these gates assert pixels. Both agreed today, on every
assertion in 23 gates. They are two version streams and have no obligation to
keep agreeing. The bundled one is pinned by `package.json` and is the same
everywhere.

`public` is gone. It carried `origin`'s URL with a stale tracking ref, so
reading the remotes reported one current and one 16 commits behind for the same
repository. `devcopy` is a different repository and stays.

`c6cb904`'s truncated message is closed as a decision rather than a fix.
Rewriting it moves every clone and every fork point of a public master to
recover one word in one message.

23/23 offline gates under the bundled Chromium, typecheck 0, lint 0, 620/620
tests.


## Pass twelve: what the manifest was handing to the site

Reading the weight gate's target led somewhere else. `dist/assets/content.js` is
228,406 bytes and the gate calls it the one cost every reader pays. Checking
that claim meant checking whether the content script pulls anything else at
runtime, since `web_accessible_resources` listed four ESM chunks.

It does not. Zero `getURL`, zero `chrome-extension`, zero dynamic import, zero
icon reference, and all 22 `url()` in the stylesheet are inline `data:` SVG.
`scripts/bundle-content.ts` re-bundles the content script as a self-contained
classic IIFE precisely so nothing is fetched at runtime, and repoints the
manifest at it. The chunks crxjs exposed were for the loader that step replaces.

So six resources were reachable from any kick.com page for no reason, at stable
URLs, which is a reliable test for whether the extension is installed. That is
worth more here than in most extensions, since this site already walls off what
it detects. The source manifest declared two of the six; both are gone, and
`patchManifest` drops whatever crxjs adds. Chrome and Firefox builds both come
out with none.

One correction on the way. A first reading reported `content.js` at 200,874 and
called it a 12 percent drop from the gate's reference of 228,460, unnoticed
because the gate only fails upward. That was a character count against a byte
reference: the file holds multi-byte UTF-8 and is 228,406 bytes. No drop, no
missing code, and the gate was measuring correctly.

**The change had no gate behind it.** Every offline gate mounts components by
hand in a fabricated page. None loads the extension, reads the manifest, or
exercises the path Chrome injects through, and the harnesses that do open the
real kick.com. So `extension-load` intercepts `https://kick.com/**` and serves a
local fixture: `content_scripts` matches on the URL, not on what answers, so the
document keeps its kick.com URL, Chrome injects, and nothing leaves the machine.
It asserts the script ran and that the manifest exposes nothing. Witnesses:
pointing `content_scripts` at a missing file, and re-exposing one chunk.

It needs a visible window. Measured: `headless: true` loads no extension at all,
giving a page with no injected style. The window is pushed to -2400,-2400 so the
suite does not flash a browser at the maintainer on every run. The first witness
did put a Chrome error dialog on kil's screen before that was settled.

24/24 offline gates, typecheck 0, lint 0, 620/620 tests.


## Pass thirteen: the thing the product is for

Twenty-four gates measured drawing, geometry, keyboard, contrast, weight, dead
selectors and the loading path. None of them checked that a message gets
translated. `chat-live` calls `inject()` directly and measures rendering; the
620 unit tests stub at module boundaries; the end-to-end path existed only in
live harnesses that open the real kick.com.

`translate-offline` loads the real extension, serves a chat fixture taken from
`observer.test.ts` rather than invented, and answers the engine with a string
that appears nowhere else, so finding it proves where it came from. The chain it
crosses is the point: Chrome loads the extension, the content script arrives,
the observer picks the right container out of two that match, the pipeline talks
to the service worker, the Google adapter reads the response shape, and the
render lands under the right message. It reports `ESZZTRADUCTIONZZ` and the
engine called once for two messages, which is the batching working.

**A correction, and it lands on yesterday's commit.** Playwright's glob does not
match `*.kick.com` against `kick.com`: `*` wants at least one character before
the dot. So `extension-load`'s document navigation went to the real site while
its subresources were intercepted, which is exactly why it printed "70
interceptions" and read as correct. The commit that added it says nothing leaves
the machine. That was false when it was written. A regular expression takes both
forms, and with the fixture actually served the interception count drops to
zero, because a local page asks for nothing.

**What the gate catches, and what it does not.** Breaking `font-normal` in
`selectors.ts` turns it red, so the chain answers to a real product regression.
But the unit tests catch that one too, four of them, so that witness proves the
gate works rather than that it covers new ground. One candidate for unique
coverage was measured and refused: removing
`https://translate.googleapis.com/*` from `host_permissions` leaves it green,
because the interception answers before the permission matters. Both facts are
in the file's header. Its value is the wiring, which nothing else offline
crosses, and it is not a statement about declared permissions.

25/25 offline gates, typecheck 0, lint 0, 620/620 tests.


## Pass fourteen: the chain that keeps translation alive

Yesterday's end-to-end gate could not show it caught anything the unit tests
missed. This one can, and the same interception made it cheap.

`live-fallback`'s header carries the reason it was live-only: its first version
routed with `page.route` and measured nothing, because in MV3 the translation
requests leave from the service worker, so it kills providers at the DNS
resolver instead. `ctx.route`, at the context level, does see those requests.
That was measured rather than assumed the moment the first gate reported the
engine called once.

So `--bascule` returns 429 from the first engine, the shape `google.ts` reads as
`rate_limit` and the reason a real reader falls over mid-stream, and a canned
answer from the second. It asserts both were called and that the text on screen
came from the second. It reports `google, google, mymemory, mymemory` and
`AUZZREPLIZZ`.

**The witness that settles the question.** Truncating the runtime cascade to a
single provider, one `.slice(0, 1)` in `background/translator/index.ts`, leaves
620 of 620 unit tests green and turns this gate red. Removing the chain from the
default settings is caught by one unit test, but that test asserts a constant:
it says the list has three entries, not that the second one is ever reached.

That is the difference worth paying runtime for. Everything else added in the
last two passes was justified by what it exercises; this one is justified by a
defect it catches alone.

26/26 offline gates, typecheck 0, lint 0, 620/620 tests.


## Pass fifteen: a real bug, found behind four wrong accusations

The plan was to make `live-recycle`'s central check runnable offline by
provoking the recycling instead of waiting for a busy chat to do it. Getting
there took four probes that accused working code, and the product was right
every time.

**One.** The row driver rewrote every previously placed row on each step, which
destroys what the extension inserted into them. It reported one translation in
eight while the engine had been called eight times.

**Two.** The mock read a single `q` and returned a single segment. Google's
adapter joins a batch with newlines into one `q` and splits the answer by lines,
so every line after the first came back identical to its original. The product
dropped them and said so on the line, in words: "the translation came back the
same as the original". I read that message, wrote a fix for multiple `q`
parameters, and it was still the wrong shape.

**Three.** Reading the skip reason off the row rather than off the injection
target returned null, and I took a missing attribute for a missing decision.
That produced a confident finding, written down mid-pass, that bursts of chat
messages were being silently dropped. With the mock finally faithful, phase one
of this gate translates nine rows out of nine. There is no burst defect. The
claim was mine, not the product's.

**Four.** The first witness for the burst hypothesis exited 1 on a Playwright
timeout rather than on the assertion, because removing the filter clear also
hid the element clicked next.

**What was actually there.** Kick's virtual scroller reuses a row by replacing
its contents. The row is then the mutation TARGET and never an added node, and
the observer collected candidates from added nodes and their descendants only.
Eight recycled rows: no translation, no reason on the line, no provider call.
The comment sitting directly above that loop says the recycling case is covered
by watching childList with subtree, which is true of the events and false of the
handling.

`enclosingMessageRow` walks up from the mutation target. Re-processing a row
whose text has not changed costs nothing, because `process` returns on the id
the row already carries, which is also what stops our own insertions from
looping.

Two witnesses. The gate goes from 9 of 9 to 1 of 9 with the walk removed. And a
unit test now fails without it, which required the test double to stop throwing
away the `MutationObserver` callback it was handed: no test could deliver a
mutation, so nothing in 620 tests could reach that branch at all.

The gate's own assertion needed hardening too. It failed only at zero and
therefore passed on one translation for nine rows, which is the exact failure it
exists to catch.

27/27 offline gates, typecheck 0, lint 0, 621/621 tests.


## Pass sixteen: the failure that says nothing

Changing channel on Kick is an app navigation: the URL moves, the chat is
remounted, the page never reloads. If the extension does not rebind, every
message after that is left alone, with no error and no reason on any line. It
was checked only by `live-nav`, on the real site.

One detail decided how to simulate it. The content script patches
`history.pushState` to notice the change, but a content script runs in an
isolated world, and Kick's router calls the main world's `pushState`, where that
patch does not exist. So the harness navigates from the main world, as the site
does, rather than from anywhere the patch would flatter it.

The product survives: the container watcher notices its container left the
document and rescans, and the message posted after the remount is translated.
That is the answer this pass was looking for, and it is a negative result worth
the same as a positive one.

The gate earns its place on what it alone can see. Disabling the rescan leaves
621 of 621 unit tests green and turns this gate red. Nothing else offline can
tell the difference between an extension that keeps working after a channel
change and one that quietly stopped.

28/28 offline gates, typecheck 0, lint 0, 621/621 tests.


## Pass seventeen: the number the listing sells

Both store listings sell hover mode on a figure: roughly ten times less
consumption on a fast chat. The figure only means anything if nothing is
requested before the pointer arrives, and only a real browser can say whether
that is true. `armHoverTranslate` even carries a comment about a guard that is
correct in a browser and invisible to every unit test.

The mode sets `displayStyle` from the extension's own service worker, the one
context in reach that has `chrome.storage`, then reloads. It asserts zero engine
calls after the message lands, hovers, and asserts one. It reports 2 rows armed,
0 calls before, 1 after.

Clean coverage, measured: short-circuiting the arming so the line translates
immediately leaves 621 of 621 unit tests green and turns this gate red on three
counts at once, including the one that matters, one call before any hover.

**And it caught a fixture that had been lying quietly.**
`pickInjectionTarget` looks for `div.w-full.min-w-0.shrink-0` first, then
`div.group`, then the row's first element child. The fixture had neither
wrapper, so the target was the username button: every translation in every mode
before this was being injected inside the username. It was on screen the whole
time, in output I read and quoted, as `pseudo1ESZZTRADUCTIONZZ:...`, and I took
the concatenation for a rendering quirk of my own probe. Hover mode is what made
it undeniable, because it armed the username instead of the message.

The recycling finding is unaffected: there the observer never delivered the row
at all, so where the translation would have gone never came up.

29/29 offline gates, typecheck 0, lint 0, 621/621 tests.


## Pass eighteen: the language a reader picks

A reader picks a language in the bar. If that does not reach the page, nothing
happens until they reload, which reads as a broken extension. `watchSettings`
exists for it and nothing offline exercised it.

The first attempt wrote `{ targetLang: 'fr' }` straight into `chrome.storage`
from the extension's service worker and the next batch still went out asking for
English. That is not a finding about the product: a partial settings object is
not what the bar writes, and there is no reason for the page to honour one.
Driving the product's own control instead, through `poserLangueCible`, the next
batch asks for French.

The assertion reads the `tl` parameter the engine actually received rather than
a label on screen, so it is about the language requested. Clean coverage,
measured: cutting `pipeline.updateSettings(next)` leaves 621 of 621 unit tests
green and turns this gate red on exactly that sentence.

Two of my own probes needed fixing on the way, both the same kind of mistake as
earlier passes. Rows were placed at an index the previous message already
occupied, so one of two test messages was silently never added. And the wait
before the language change watched for any translation rather than this
message's, so the seed satisfied it and the real request landed on the wrong
side of the measurement.

30/30 offline gates, typecheck 0, lint 0, 621/621 tests.

## Left behind

`.agent/state.mjs`, `.agent/PROMPT.md`, `.agent/PLAN.md`, this file.
