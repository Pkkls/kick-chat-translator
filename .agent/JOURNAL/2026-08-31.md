# 2026-08-31

## The counters, read at last

Thirty counters were instrumented under `__KT_METRICS__`, `summarize()` produced
p50 and p95, `readAllMetrics()` was exported, and `latency.mjs` was written to
read them. It needs real traffic on kick.com, so it never ran. Everything was
collected and nothing was read.

`metrics-offline` drives traffic through the real extension on a locally served
page and reads `chrome.storage.local` at the source, computing the percentiles
once from the raw reservoirs. `npm run metrics` builds with metrics, runs it,
and puts `dist` back. It refuses green when it read nothing, because a reader
that reports zero everywhere looks exactly like a perfect product.

## What the code asked, and what the numbers said

**The share of chat that never reaches an engine.** 24 translated, 12 skipped
with a reason on the line, none left without one: 5 emoji or laughter, 4 English
with `ignoreEnglish` on, 3 slang. `minTextLength` never fired at all, its
two-character default sitting below anything a chat contains. The share belongs
to the corpus, not to the world, and no real corpus exists here; what is
measured is the classifier, and reading each row's own verdict back off the page
is what found the gap below.

**`leg.inject`, "cheap in theory, never measured".** n=34, p50 0ms, p95 0ms,
max 1ms. Cheap in fact. Closed.

**Which cache tier earns its size.** Inside a page the question cannot be asked:
memory answers first, so it took 10 hits and the service worker tier saw only
misses. After a reload, where memory restarts empty and the worker does not:
memory 0 hits and 9 misses, worker 9 hits and 0 misses, zero provider calls for
8 already-seen messages. Neither tier is dead weight.

**The join-and-split fallback.** Zero over six chain attempts. The harness
cannot raise it either, since the mock is faithful to the newline protocol, and
that limit is in its header rather than implied by a green run.

**local-first against cloud** stays open, and the plan says why: `e2e.local`
never records because no on-device model is ready in this Chromium, which
`local.pair.downloadable` at 14 states exactly.

## Three defects the numbers exposed

`(xd)+` was translated while every other repeated laughter form is skipped:
`x+d+` catches xd, xdd and xddd and misses xdxd, where jaja, haha, rsrs, huehue,
lolol, hehe and hihi are all handled. Corpus goes from 7 provider calls to 6.

**A batch declared one message's source language for all of them.** The
coalescer groups by target and nothing else, and `batchCall` built its joined
request as `{ ...reqs[0], text: joined }`. A request went out as `sl=ja`
carrying a Japanese line and an Arabic one, in a file whose own comment explains
that a wrong `sl` makes the engine translate from the wrong language. Disagreeing
hints now declare nothing.

**Malay and Hebrew were never identified**, two of the 42 offered languages, and
the listing sells Hebrew in its right-to-left claim. Malay because franc emits
`zlm` and the table knew `msa` and `zsm`; Hebrew because franc-min does not
cover it and the script pre-check had every other unambiguous script but not
that one. The first framing was wrong and the harness corrected it: an
unidentified language is not dropped, it goes out with `sl=auto`, so those
messages were translated all along. What the fix buys is a correct source
declared to the engine and a correct badge shown to the reader.

## One optimisation measured and thrown away

The 40ms coalescing floor sits under a comment calling its branch pure delay,
which invites removing it. Removed and measured: at 1.2s spacing the wait went
from 44ms to 0 at p50 with the same 15 provider calls, so the window bought
nothing there. On the same corpus arriving in bursts of three, calls went from 6
to 15 for the same 24 translations, 2.5x the requests against an endpoint that
soft-bans per IP. A burst does not reach `submit()` in one tick: each row passes
the observer, language detection and a cache lookup first, so a `setTimeout(0)`
fires before its siblings. Reverted, with the measurement written beside the
line that invited it.

## Weight

Measured in the shipped artefact, after a first reading from an esbuild
metafile claimed 48.8 percent while its `bytesInOutput` disagreed with the
source file by more than a factor of two. `content.js` is 228896 bytes; franc's
trigram data is 100394 of them across 65 blocks, 43.9 percent; the inlined
stylesheet is 30474, 13.3 percent.

Of the 61 languages franc-min can return, 21 map to something the product
translates and 40 do not, carrying about 61 KB. Not trimmed, and the reason is
the interesting part: dropping a language franc would have chosen makes it
choose a different one, and a mapped wrong answer is worse than an unmapped
right one. The experiment that would settle it is a before-and-after on
detection quality, not a diff on the bundle size.

37/37 offline gates, typecheck 0, lint 0, 645/645 tests.
